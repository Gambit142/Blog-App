<!DOCTYPE html>
<html>
  <head>
    <title>BlogApp</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_pack_tag 'application', 'data-turbolinks-track': 'reload' %>
    <script src="https://kit.fontawesome.com/29d7bf5b00.js" crossorigin="anonymous"></script>
  </head>

  <body>
    <nav class="nav-bar flex justify-center items-center">
      <ul class="flex mx-30 no-list">
        <li class="mr-10"><%= link_to 'Home', root_url, class: 'font-xl' %></li>
        <% if user_signed_in? %>
          <li class="mr-10">
            <%= link_to 'Sign Out', destroy_user_session_url, method: :delete, class: 'font-xl' %>
          </li>
          <li class="mr-10">
            <%= link_to 'Create Post',  new_user_post_path(current_user.id), class: 'font-xl' %>
          </li>
        <% else %>
          <li class="mr-10">
            <%= link_to 'Sign In', new_user_session_url, class: 'font-xl' %>
          </li>
          <li class="mr-10">
            <%= link_to 'Sign Up',  new_user_registration_url, class: 'font-xl' %>
          </li>
        <% end %>
        <li class="mr-10"><%= link_to 'Back', :back, class: 'font-xl' %></li>
      </ul>
    </nav>

    <% flash.each do |name, msg| -%>
      <%= content_tag :p, msg, class: name %>
    <% end -%>

    <%= yield %>
  </body>
</html>
